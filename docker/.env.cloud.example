# ==============================================
# CLOUD ENVIRONMENT VARIABLES
# ==============================================
# Copia este archivo a .env.cloud y llena los valores reales.
# NUNCA commitear .env.cloud con credenciales reales.
#
# Uso:
#   cp docker/.env.cloud.example docker/.env.cloud
#   # Editar docker/.env.cloud con valores reales
#   make db-migrate-cloud
#   make db-recreate-cloud  # (destructivo)

# --- PostgreSQL (Neon / Supabase / etc.) ---
# Opcion 1: URI completo (preferido para servicios cloud)
POSTGRES_URI=postgres://user:password@host:5432/dbname?sslmode=require
# Opcion 2: Variables individuales
# POSTGRES_HOST=ep-xxx.us-east-1.aws.neon.tech
# POSTGRES_PORT=5432
# POSTGRES_USER=neondb_owner
# POSTGRES_PASSWORD=your-password
# POSTGRES_DB=edugo
# POSTGRES_SSLMODE=require

# --- MongoDB (Atlas / etc.) ---
# Opcion 1: URI completo (preferido para Atlas con mongodb+srv://)
MONGO_URI=mongodb+srv://user:password@cluster.mongodb.net/?appName=MyApp
MONGO_DB_NAME=edugo
# Opcion 2: Variables individuales (para MongoDB standalone)
# MONGO_HOST=localhost
# MONGO_PORT=27017
# MONGO_USER=admin
# MONGO_PASSWORD=your-password

# --- Opcionales ---
# APPLY_MOCK_DATA=false    # No aplicar datos de prueba en cloud
# FORCE_MIGRATION=true     # Recrear todo desde cero (destructivo)
# POSTGRES_ONLY=true       # Solo migrar PostgreSQL
# MONGO_ONLY=true          # Solo migrar MongoDB
